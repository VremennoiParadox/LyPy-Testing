# LyPy package

Documentation, build steps, and feature list for this project live in the **repository root** [README.md](../README.md).

Quick start from repo root:

```bash
pip install -r LyPy/requirements.txt
cd LyPy && py -3 generate_icons.py && cd ..
python LyPy/main.py
```
